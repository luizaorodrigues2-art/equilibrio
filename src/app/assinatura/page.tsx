import { redirect } from "next/navigation";

// Assinatura temporariamente desativada — redireciona para a Home.
// (O conteúdo original permanece no histórico do Git para reativação futura.)
export default function AssinaturaPage() {
  redirect("/");
}
